{
	"tasks": {
		"install_dash": "deno install -A --reload -f -n dash_compiler https://raw.githubusercontent.com/bridge-core/deno-dash-compiler/main/mod.ts",
		"watch": "dash_compiler watch --o preview",
		"build": "dash_compiler build --o preview"
	}
}