{
  "tasks": {
    "setup": "deno install -A -n dash_compiler https://deno.land/x/dash_compiler/mod.ts",
    "watch": "dash_compiler build --m development --o preview && dash_compiler watch --m development --o preview --r 8080",
    "build": "dash_compiler build"
  } 
}
